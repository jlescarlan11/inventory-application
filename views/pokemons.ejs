<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <% if (locals.pokemons) {%> <% pokemons.forEach(pokemons => {%>
    <p>
      <%= pokemons.name %> | <%= pokemons.type1_id %> || <%= pokemons.type2_id
      %>
    </p>
    <%})%> <%}%>
  </body>
</html>
